# Entity Framework Core (EF Core)

Console-based sample showing:
- Fluent API configuration
- LINQ join and aggregate queries
- `ExecuteUpdateAsync` and `ExecuteDeleteAsync`
- SQLite in-memory setup for a relational demo
