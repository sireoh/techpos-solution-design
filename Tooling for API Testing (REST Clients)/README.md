# Tooling for API Testing (REST Clients)

Contains a file-based REST Client example for VS Code and Visual Studio.

See `api-tests.http` for sample requests against the Web API projects in this workspace.
